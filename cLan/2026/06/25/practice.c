#include <stdio.h>

int main(void){

  // 이름, 나이, 키를 변수에 저장하고 출력하는 프로그램을 작성하시오.
  // char name[20];
  // int age;
  // float height;

  // printf("이름을 입력해주세요: ");
  // scanf("%s", name);
  // printf("나이를 입력해주세요: ");
  // scanf("%d", &age);
  // printf("키를 입력해주세요: ");
  // scanf("%f", &height);

  // printf("입력하신 이름은 : %s 입니다. \n", name);
  // printf("입력하신 나이는 : %d 입니다. \n", age);
  // printf("입력하신 키는 : %.1f 입니다. \n", height);

  // 두 정수를 입력받아 덧셈, 뺄셈, 곱셈, 나눗셈 결과를 출력하는 프로그램을 작성하시오.
  // int num1;
  // int num2;

  // printf("두개의 정수를 입력해주세요.");
  // scanf("%d %d", &num1, &num2);


  // printf("덧샘 : %d + %d = %d \n", num1, num2, (num1 + num2));
  // printf("뺄셈 : %d - %d = %d \n", num1, num2, (num1 - num2));
  // printf("곱셈 : %d * %d = %d \n", num1, num2, (num1 * num2));
  // printf("나눗셈 : %d / %d = %.1f \n", num1, num2, (float)num1 / num2);


  // 반지름을 입력받아 원의 넓이와 둘레를 계산하여 출력하시오.
  // 단, 원주율 PI는 const로 선언하여 사용하시오.
  // const double PI =  3.14;
  // double r;
  // double area;
  // double round;

  // printf("반지름을 입력하세요: ");
  // scanf("%lf", &r);

  // area = PI * r * r;
  // round = 2 * PI * r;

  // printf("원의 넓이: %.1f \n", area);
  // printf("원의 넓이: %.1f \n", round);

  // 총 초 second를 입력받아 몇 시간, 몇 분, 몇 초인지 변환하여 출력하는 프로그램을 작성하시오.

  // int sec;
  // int min = 0;
  // int hour = 0;
  // printf("초를 입력해주세요: ");
  // scanf("%d", &sec);

  // if(sec >= 60){
  //   min = sec / 60;
  //   sec = sec % 60;
  //   if(min >= 60){
  //     hour = min / 60;
  //   }
  // }

  // printf("현재 시각은 %d시 %d분 %d초 입니다. \n", hour, min, sec);



  // 키(cm)와 성별을 입력받아 표준 체중을 계산하고, 현재 체중(kg)과의 차이를 출력하는 프로그램을 작성하시오.

  return 0;
}